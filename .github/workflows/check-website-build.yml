name: Check Website Build
on:
  pull_request:
    branches:
      - "*"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Call reusable build workflow (no deploy)
        uses: OWASP/owasp-mastg/.github/workflows/build-website-reusable.yml@main # ./.github/workflows/build-website-reusable.yml
        with:
          deploy: false